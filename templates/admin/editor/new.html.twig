{% extends 'base.html.twig' %}

{% set action = app.current_route == 'app_admin_editor_new' ? 'Ajout' : 'Édition' %}

{% block title %}{{ action }} d'éditeur{% endblock %}

{% block body %}

<section class="container " style="min-height:100vh">
    <div class="row justify-content-center mt-5 py-5 " >
        <div class="col-6">
            <h1>Ajout d'éditeur</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Envoyer</button>
            {{ form_end(form) }}
        </div>
    </div>
</section>
{% endblock %}
